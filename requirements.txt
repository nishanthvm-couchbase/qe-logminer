# Test Analysis pipeline dependencies.
# (droid is a separate CLI installed on the machine — NOT a pip package.)
requests>=2.28.0,<3
couchbase==4.5.0

# Near-duplicate (vector) dedup (VECTOR_DEDUP=1) — pick ONE embedding backend:
#   EMBED_PROVIDER=openai  (recommended) — no extra pip (uses `requests` above); set
#                          OPENAI_API_KEY. Model text-embedding-3-small, ~$0.02/1M tok.
#   EMBED_PROVIDER=local   — on-slave, no API cost:  pip install sentence-transformers
# If the chosen backend is unavailable, the feature auto-disables (pipeline unchanged).
